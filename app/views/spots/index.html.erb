<% content_for(:page) do %>
  Spots
<% end %>
<% if current_page?(root_path) %>
  <%= render "shared/homepage" %>
<% end %>

<div class="container">
  <div class="filters">
    <%= render "filters" %>
  </div>

  <div class="spot-cards">
    <% @spots.each do |spot| %>
      <%= render "cards", spot: %>
    <% end %>
  </div>
</div>
